apiVersion: v1
#kind: apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fast_pvc
spec:
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: fast
